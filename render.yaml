services:
  - type: web
    name: maxkb-enterprise
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: https://lumen-maxkb-api.ngrok.app
